---
title: anymd
slug: pdf-reader-mcp
category: mcp
description: >-
  Local MCP server and CLI, formerly PDF Reader MCP, that converts PDF, DOCX,
  PPTX, XLSX/CSV, EPUB, HTML and web URLs, images, and audio/video files into
  clean Markdown for Claude, with read, search, and inspect tools.
cardDescription: Turn any local file or URL into clean, page-cited Markdown for Claude. No API key.
seoTitle: anymd (formerly PDF Reader MCP) MCP Server for Claude
seoDescription: >-
  Configure anymd, formerly PDF Reader MCP, with Claude and other MCP clients
  to convert PDFs, Office documents, spreadsheets, EPUB, HTML, images, and
  audio/video files to Markdown, search across them, and inspect PDF pages
  locally without an API key.
author: Sylphx
authorProfileUrl: "https://github.com/SylphxAI"
submittedBy: shtse8
submittedByUrl: "https://github.com/shtse8"
dateAdded: "2026-06-06"
brandName: anymd
brandDomain: github.com
repoUrl: "https://github.com/SylphxAI/anymd"
documentationUrl: "https://sylphxai.github.io/anymd/"
packageUrl: "https://registry.npmjs.org/%40sylphx%2Fanymd"
installable: true
installCommand: "npx -y @sylphx/anymd"
usageSnippet: "Ask Claude to use anymd to read an approved file, folder, or URL as Markdown, or to search a folder of documents for a phrase and cite the pages it found."
copySnippet: |-
  {
    "mcpServers": {
      "anymd": {
        "command": "npx",
        "args": ["-y", "@sylphx/anymd"]
      }
    }
  }
configSnippet: |-
  {
    "mcpServers": {
      "anymd": {
        "command": "npx",
        "args": ["-y", "@sylphx/anymd", "--allow-dir=/Users/you/Documents/approved"]
      }
    }
  }
estimatedSetupTime: 5 minutes
difficulty: beginner
prerequisites:
  - Node.js 18 or newer and npx available to the MCP client runtime; npm installs the native binary for macOS, Linux, or Windows.
  - Optional local tools for extra capabilities, used only when installed - tesseract for OCR, ffprobe/ffmpeg for audio and video, whisper.cpp for transcripts.
  - Approved local directories when processing sensitive material.
safetyNotes:
  - anymd reads local files and folders, and fetches web URLs that are passed to it. Use --allow-dir (or MCP_PDF_ALLOWED_DIRS) to confine it to approved directories.
  - URL fetches block private and loopback addresses, and every redirect hop is checked again.
  - Optional external tools (tesseract, ffprobe, whisper.cpp) are run without a shell, with a timeout and an output cap.
  - Conversion quality can vary on scanned, encrypted, or unusually laid-out documents; verify important tables and figures.
privacyNotes:
  - Documents are processed locally and are not uploaded; a URL is fetched only when you pass one.
  - File paths, URLs, extracted text, tables, and metadata (including EXIF GPS data from images) become visible to the MCP client and model provider.
  - Remote URL fetching reveals the requested URL and request metadata to the upstream host.
sourceUrls:
  - "https://github.com/SylphxAI/anymd/blob/main/README.md"
  - "https://github.com/SylphxAI/anymd/blob/main/package.json"
  - "https://github.com/SylphxAI/anymd/blob/main/SECURITY.md"
  - "https://github.com/SylphxAI/anymd/blob/main/LICENSE"
  - "https://sylphxai.github.io/anymd/"
tags:
  - pdf
  - markdown
  - document-processing
  - extraction
  - tables
  - ocr
keywords:
  - anymd
  - PDF Reader MCP
  - file to Markdown MCP
  - PDF to Markdown MCP
  - DOCX to Markdown MCP
  - document search MCP
robotsIndex: true
robotsFollow: true
---

## Content

anymd, formerly PDF Reader MCP, is a local Model Context Protocol server and
CLI that turns files into clean Markdown for agents. It reads PDF, Word
(`.docx`), PowerPoint (`.pptx`), Excel/ODS/CSV/TSV, EPUB, HTML and web URLs,
Markdown/text/JSON, images (dimensions, EXIF, optional OCR), and audio/video
metadata and subtitles. The engine is native Rust, and it needs no API key or
cloud service.

For PDFs, anymd rebuilds words from glyph positions, keeps two-column papers in
reading order, and recovers tables, including borderless ones. Every page carries a
`<!-- page N -->` anchor that Claude can cite.

## Source Review

- https://github.com/SylphxAI/anymd
- https://github.com/SylphxAI/anymd/blob/main/README.md
- https://registry.npmjs.org/%40sylphx%2Fanymd
- https://github.com/SylphxAI/anymd/blob/main/SECURITY.md
- https://github.com/SylphxAI/anymd/blob/main/LICENSE

The repository was renamed from `SylphxAI/pdf-reader-mcp` to `SylphxAI/anymd`.
`@sylphx/pdf-reader-mcp` still installs and runs anymd, and the old tool names
(`read_pdf`, `search_pdf`) still work for the current major version.

## Features

- npm package `@sylphx/anymd`. It runs as a stdio MCP server with `npx -y @sylphx/anymd`.
- `read` turns a file, URL, or folder into Markdown, with page ranges, a token budget, and a cursor for large documents.
- `search` finds text across files, folders, and URLs, and falls back to BM25-ranked passages.
- `inspect` renders PDF pages, extracts regions, runs OCR on pages, returns structure with geometry, and compares documents.
- The same binary works as a CLI: `anymd report.pdf > report.md`.
- `--allow-dir` directory confinement, and SSRF-safe URL fetching.
- Binaries for macOS, Linux, and Windows.
- MIT license.

## Installation

```json
{
  "mcpServers": {
    "anymd": {
      "command": "npx",
      "args": ["-y", "@sylphx/anymd"]
    }
  }
}
```

With Claude Code: `claude mcp add anymd -- npx -y @sylphx/anymd`. Add
`--allow-dir=/path/to/docs` to keep the server inside one folder.

## Use Cases

- Convert a PDF, deck, or spreadsheet to Markdown before summarizing it.
- Search a folder of contracts or papers for a phrase and cite the pages.
- Read a web article without navigation, banners, or sidebars.
- Extract tables from reports and invoices as Markdown tables.
- Read image EXIF data or video chapters and subtitles.

## Safety and Privacy

anymd can expose everything a document contains, including hidden metadata,
comments, and image GPS data. Use `--allow-dir` for private workflows, and
verify extraction quality before relying on important tables or scanned-document
text.

## Duplicate Check

Existing content includes MarkItDown, Kreuzberg, and Markdownify. This entry
already covered `SylphxAI/pdf-reader-mcp`. It is updated in place because that
project was renamed to anymd and now handles every format listed above.
